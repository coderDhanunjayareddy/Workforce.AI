# Demo Transcripts

Scenario transcripts are structured in `src/demo/scenarios` so playback can synchronize by timestamp.
This folder is reserved for future external transcript files if narration is managed outside the application bundle.
